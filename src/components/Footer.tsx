import styles from './Footer.module.css';

const Footer = () => {
  return ( 
    <footer className={`${styles.footer} `}>
      <div>Created by Rubeun</div>
    </footer>
  );
}

export default Footer;